/**
 * 우선순위큐(Priority Queue)
 * :우선순위의 개념을 큐에 도입한 자료 구조
 *  데이터들이 우선순위를 가지고 있고 우선순위가 높은 데이터가 먼저 나간다
 *
 * 이용사례
 * a.시뮬레이션 시스템
 * b.네트워크 트래픽 제어
 * c.운영체제에서의 작업 스케줄링
 * d.수치 해석적인 계산
 *
 * 우선순위 큐는 배열, 연결리스트, 힙 으로 구현이 가능하고,
 * 이 중에서 힙(heap)으로 구현하는 것이 가장 효율적이다
 */
public class PriorityQueue {
}
