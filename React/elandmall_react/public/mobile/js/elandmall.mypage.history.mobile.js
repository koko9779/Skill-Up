(function () {
/*	
	history.pushState(null, null, '');
	window.onpopstate = function(event) {
		var prevUrl = document.referrer;
		//뒤로가기를 한 페이지가 메인 페이지가 아니면 메인으로, 아니면 뒤로가기
		if(prevUrl.indexOf('/mypage/initMypageMain.action') < 0){
			elandmall.hdLink('MYPAGE');
		}else{
			history.back();
		}
	};
*/
	
}());